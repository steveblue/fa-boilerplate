(function( define ) {
  "use strict";

  define( [], function ()
  {
    var MainController = function( $rootScope, $scope, $famous ){


    };

    return [ "$rootScope", "$scope", "$famous", MainController ];

  });

}( define ));

